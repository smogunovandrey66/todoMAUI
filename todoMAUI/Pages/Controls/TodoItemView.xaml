<?xml version="1.0" encoding="utf-8" ?>
<Border xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:todoMAUI.Models"
             xmlns:pageModel="clr-namespace:todoMAUI.PageModels"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:local="clr-namespace:todoMAUI.Utilities"
             x:Class="todoMAUI.Pages.Controls.TodoItemView"
             x:DataType="models:TodoItem"
             StrokeThickness="0"
             Padding="0,2">
    <Border.Resources>
        <local:DoneToSymbolConverter x:Key="DoneToSymbolConv"/>
    </Border.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.GestureRecognizers>
            <TapGestureRecognizer
                Command="{Binding ItemClickCommand, Source={RelativeSource AncestorType={x:Type pageModel:TodoItemsModel}}, x:DataType=pageModel:TodoItemsModel}"
                CommandParameter="{Binding .}"/>
            <TapGestureRecognizer
                Tapped="TapGestureRecognizer_Tapped"/>
        </Grid.GestureRecognizers>

        <!--<CheckBox
            x:Name="CheckBoxDone"
            Grid.Column="0"
            IsChecked="{Binding IsDone}"
            CheckedChanged="CheckBoxDone_CheckedChanged">
            <CheckBox.GestureRecognizers>
                <TapGestureRecognizer
                    Command="{Binding ItemClickCommand, Source={RelativeSource AncestorType={x:Type pageModel:TodoItemsModel}}, x:DataType=pageModel:TodoItemsModel}"
                    CommandParameter="{Binding .}"/>
            </CheckBox.GestureRecognizers>
        </CheckBox>-->
        <Label 
            Text="{Binding IsDone, Converter={StaticResource DoneToSymbolConv}}"
            Grid.Column="0" 
            VerticalOptions="Center"
            Margin="10,0,10,0"
            />
        <Label 
            Text="{Binding Title}"
            Grid.Column="1"
            VerticalOptions="Center"
            TextDecorations="{Binding Decorations}"/>
    </Grid>
</Border>